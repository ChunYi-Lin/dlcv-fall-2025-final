Decide the object type for <target_mask> based on the sentence context.

Valid object types (return exactly ONE word, lowercase):
pallet
transporter
shelf
buffer

Rules:
- Output ONLY one of the 4 words above.
- Do NOT output any extra text, punctuation, or explanations.

Examples:
Sentence: Among the buffer zones <target_mask> <mask> <mask>, which one is closest to <mask>?
Output: buffer

Sentence: Which pallet is closest to the shelf <target_mask>?
Output: shelf

Your turn:
Sentence: <input>
Output:
